import { Search } from 'lucide-react';import { useMemo,useState } from 'react';import { ToolCard } from '../components/ToolCard';import { api } from '../lib/api';import { useAsync } from '../lib/useAsync';export function ToolsPage(){const{data=[]}=useAsync(api.tools,[]);const[q,setQ]=useState('');const[category,setCategory]=useState('All');const cats=['All','Core chart','Timing','Relationships','Purpose','Research'];const visible=useMemo(()=>data?.filter(t=>(category==='All'||t.category===category)&&`${t.title} ${t.description}`.toLowerCase().includes(q.toLowerCase()))||[],[data,q,category]);return <section className="section page-top"><div className="container"><div className="page-hero compact-hero"><div className="eyebrow">Tool library</div><h1>One profile. Many lenses.</h1><p>Every module calls a stable API contract.</p></div><div className="tool-controls"><label className="search-field"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search tools…"/></label><div className="filter-pills">{cats.map(c=><button key={c} className={c===category?'filter-pill active':'filter-pill'} onClick={()=>setCategory(c)}>{c}</button>)}</div></div><div className="card-grid tools-grid">{visible.map(t=><ToolCard key={t.id} tool={t}/>)}</div></div></section>}
