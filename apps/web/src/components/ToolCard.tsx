import type { ToolSummary } from '@vedanova/contracts';import { ArrowUpRight,LockKeyhole } from 'lucide-react';import { Link } from 'react-router-dom';export function ToolCard({tool}:{tool:ToolSummary}){return <Link to={tool.path} className="tool-card card"><div className="tool-card-head"><span className="eyebrow">{tool.eyebrow}</span>{tool.premium&&<span className="premium-pill"><LockKeyhole size={12}/> Preview</span>}</div><h3>{tool.title}</h3><p>{tool.description}</p><div className="tool-card-foot"><span>{tool.category}</span><ArrowUpRight size={18}/></div></Link>}
