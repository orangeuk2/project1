import type { Expert } from '@vedanova/contracts';
import { BadgeCheck,MessageCircle,Star } from 'lucide-react';
import { Link } from 'react-router-dom';
export function ExpertCard({expert}:{expert:Expert}){return <article className="card expert-card"><div className="expert-top"><div className="avatar-wrap"><img src={expert.avatar} alt={expert.name}/><span className={expert.online?'presence online':'presence'}/></div><div className="expert-meta"><div className="expert-name-line"><h3>{expert.name}</h3>{expert.verified&&<BadgeCheck size={15}/>}</div><p>{expert.role}</p></div></div><p className="signature">{expert.signature}</p><div className="tag-row">{expert.tags.slice(0,3).map(t=><span className="tag" key={t}>{t}</span>)}</div><div className="expert-stats"><span><Star size={13}/> {expert.rating}</span><span>{expert.reviews.toLocaleString()} reviews</span><span>{expert.years} yrs</span><span>₹{expert.rate}/min</span></div><div className="card-actions"><Link className="button button-ghost" to={`/experts/${expert.id}`}>Profile</Link><Link className="button" to={`/chat/${expert.id}`}><MessageCircle size={15}/> Chat</Link></div></article>}
