import type { Expert } from '@vedanova/contracts';import { ArrowUpRight,BadgeCheck,MessageCircle,Star } from 'lucide-react';import { Link } from 'react-router-dom';export function ExpertCard({expert}:{expert:Expert}){return <article className="expert-card card"><div className="expert-top"><div className="avatar-wrap"><img src={expert.avatar} alt={expert.name}/><span className={expert.online?'status online':'status'}/></div><div className="expert-meta"><div className="eyebrow">{expert.specialty} · {expert.online?'Online now':'Back soon'}</div><h3>{expert.name} <BadgeCheck size={16}/></h3><p>{expert.role}</p></div></div><div className="signature">{expert.signature}</div><div className="tag-row">{expert.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div><div className="expert-stats"><span><Star size={15} fill="currentColor"/> {expert.rating}</span><span>{expert.reviews.toLocaleString()} reviews</span><span>₹{expert.rate}/min</span></div><div className="card-actions"><Link className="button button-ghost" to={`/experts/${expert.id}`}>Profile <ArrowUpRight size={16}/></Link><Link className="button" to={`/chat/${expert.id}`}><MessageCircle size={16}/> Chat</Link></div></article>}
