import type { BirthProfile } from '@vedanova/contracts';
import { Clock3,MapPin,UserRound } from 'lucide-react';
export const demoProfile:BirthProfile={name:'Demo Seeker',date:'1993-11-18',time:'07:42',place:'Mumbai, India',timezone:'Asia/Kolkata'};
export function BirthForm({value,onChange,compact=false,title}:{value:BirthProfile;onChange:(next:BirthProfile)=>void;compact?:boolean;title?:string}){
  const set=(key:keyof BirthProfile,next:string)=>onChange({...value,[key]:next});
  return <div className="birth-form-wrap">{title&&<div className="mini-title">{title}</div>}<div className={compact?'birth-grid compact':'birth-grid'}>{!compact&&<label><span>Name</span><div className="input-with-icon"><UserRound size={15}/><input value={value.name} onChange={e=>set('name',e.target.value)} placeholder="Your name"/></div></label>}<label><span>Date of birth</span><input type="date" value={value.date} onChange={e=>set('date',e.target.value)}/></label><label><span>Time</span><div className="input-with-icon"><Clock3 size={15}/><input type="time" value={value.time} onChange={e=>set('time',e.target.value)}/></div></label><label><span>Place</span><div className="input-with-icon"><MapPin size={15}/><input value={value.place} onChange={e=>set('place',e.target.value)} placeholder="City, country"/></div></label><label><span>Timezone</span><select value={value.timezone} onChange={e=>set('timezone',e.target.value)}><option>Asia/Kolkata</option><option>Europe/London</option><option>America/New_York</option><option>Asia/Singapore</option><option>Australia/Sydney</option></select></label></div></div>
}
